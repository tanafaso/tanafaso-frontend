import 'package:flutter/material.dart';

class ShowAllFriendsWidget extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text('This is the show all friends widget.'),
    );
  }
}
