import 'package:flutter/material.dart';

class ShowFriendRequestsWidget extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text('This is the show friend requests widget.'),
    );
  }
}
